import { useState, useEffect } from 'react';
import { useNavigate, useSearchParams } from 'react-router-dom';
import api from '../api/axios';

const HomePage = () => {


    return (
        <p>Hiển thị nội dung ở đây</p>
    );
};

export default HomePage;